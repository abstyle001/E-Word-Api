info:
  name: 查询所有word
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5194/word
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
